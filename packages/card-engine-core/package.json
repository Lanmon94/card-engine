{
  "name": "@card-engine/core",
  "version": "0.1.0",
  "description": "Pure TypeScript card game data models and utilities",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": ["src/"],
  "sideEffects": false,
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
